
<h1><%= @entry.word %></h1>


<%= render 'single_entry', entry: @entry %>
<%= link_to 'Edit', edit_entry_path(@entry) %>
| <%= link_to 'Delete', entry_path(@entry), method: :delete, data: {confirm: "Are you sure you want to delete the event '#{@entry.word}'? This cannot be undone!"} %>
